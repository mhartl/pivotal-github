#!/usr/bin/env ruby
$LOAD_PATH.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'pivotal-github/record'

command = Record.new(ARGV.dup)
if ARGV.include?('--debug')
  puts command.cmd
else
  exit command.run!
end